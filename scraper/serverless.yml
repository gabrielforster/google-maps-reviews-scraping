service: scraper
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs20.x
  stage: api
  timeout: 120
  environment:
    scraperPort: ${file(../offlineConfig.yml):services.scraper.lambdaPort}

functions:
  scheduled:
    handler: ports/webScraper.run
    events:
      # Invoke Lambda function every 5 minutes
      - schedule: rate(1 minute)

  mapper:
    handler: ports/mapper.handler



custom:
  serverless-offline: ${file(../offlineConfig.yml):services.scraper}


plugins:
  - serverless-esbuild
  - serverless-offline
